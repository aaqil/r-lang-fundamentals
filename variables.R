a <- 3 

# you should avoid names that contain characters like ''£, $, %, ^ *, +,., ( ), [ ], #, !, ?, <, >, and so
#on, as most of these characters are operators, for example, multiplication,power, and so on.
